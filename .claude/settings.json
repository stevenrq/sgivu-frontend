{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(node -e \"const d=JSON.parse(require('fs').readFileSync(0,'utf8'));process.stdout.write(d.tool_input?.file_path??'')\"); case \"$f\" in *.ts|*.html|*.css|*.js|*.json|*.md) cd \"$CLAUDE_PROJECT_DIR\" && npx prettier --write \"$f\" >/dev/null 2>&1 || true;; esac"
          }
        ]
      }
    ]
  }
}
